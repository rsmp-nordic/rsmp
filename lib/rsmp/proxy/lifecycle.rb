module RSMP
  module ProxyExtensions
    # Legacy shell preserved to keep require paths stable.
    # All lifecycle behaviour now lives directly on `RSMP::Proxy`.
    module Lifecycle
    end
  end
end
